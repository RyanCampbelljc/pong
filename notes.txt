MULTIPLAYER
use webRTC or peerJS in order to establish the connections
will need to write some initial server code in order to originally establish the p2p connections


todo
make font family used in html the same as the one used in the canvas rendering

probably do not need a singleplayer and multiplayer.html
can have another param for Game constructor that states the gametype (enum)
with that information can change the page title and whatever else.